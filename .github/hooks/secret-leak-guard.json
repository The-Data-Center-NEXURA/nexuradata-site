{
  "version": "1.0",
  "type": "hook",
  "event": "PreToolUse",
  "description": "Block terminal commands that would print known secret patterns to output — env vars containing API keys, tokens, secrets, or DSNs.",
  "filters": [
    {
      "tool": "run_in_terminal",
      "match": { "command": "(echo|Write-Output|cat|Get-Content|print).*(_KEY|_SECRET|_TOKEN|_DSN|_PASSWORD|API_KEY|AUTH_TOKEN|WEBHOOK_SECRET|DATABASE_URL)" }
    }
  ],
  "action": "block",
  "message": "BLOCKED: This command would print a secret value to terminal output. Secrets must never appear in logs or output. Use $env:VAR_NAME in logic without echoing it, or check the value indirectly (e.g. test if it is set, not what it contains)."
}
