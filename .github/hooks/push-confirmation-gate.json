{
  "version": "1.0",
  "type": "hook",
  "event": "PreToolUse",
  "description": "Block any terminal command that would push to a remote git repository. The user must explicitly say 'push' in chat before the agent may push.",
  "filters": [
    {
      "tool": "run_in_terminal",
      "match": { "command": "git push" }
    }
  ],
  "action": "warn",
  "message": "GIT PUSH GATE: About to push to origin. This will update the live deployment on Cloudflare Pages. Confirm by responding 'push' if you want to proceed."
}
