# Archived D1 (SQLite) Migrations

These migrations were authored when NEXURADATA used Cloudflare D1. The project has since
moved to Neon Postgres. The current canonical schema lives at
[`migrations/neon/0001_full_schema.sql`](../neon/0001_full_schema.sql).

These files are kept for historical reference only. They are not applied anywhere.
